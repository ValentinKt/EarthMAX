# Mutation to delete an eco-tip
# Deletes an eco-tip and returns confirmation
mutation DeleteEcoTip($filter: eco_tipsFilter!) {
  deleteFromeco_tipsCollection(filter: $filter) {
    records {
      id
      title
    }
  }
}