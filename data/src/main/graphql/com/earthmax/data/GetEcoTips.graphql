query GetEcoTips($category: String, $limit: Int) {
  eco_tipsCollection(
    first: $limit
  ) {
    edges {
      node {
        id
        title
        description
        category
        difficulty
        estimated_impact
        steps
        image_url
        tags
        created_at
        updated_at
      }
    }
  }
}